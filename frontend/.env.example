# Frontend (Vite) — names only. Never put OpenAI or Railway tokens here.
#
# Local development:
#   Leave both empty. `npm run dev` proxies /api and /ws → http://127.0.0.1:8000
#
# Vercel production (Project → Environment Variables → Production):
#   VITE_API_URL=https://YOUR-RAILWAY-SERVICE.up.railway.app
#   VITE_WS_URL=wss://YOUR-RAILWAY-SERVICE.up.railway.app/ws/analyze

VITE_API_URL=
VITE_WS_URL=
